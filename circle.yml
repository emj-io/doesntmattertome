machine:
  services:
    - docker

dependencies:
  post:
    - docker build -t $AWS_ACCOUNT_ID.dkr.ecr.eu-central-1.amazonaws.com/doesntmattertome/app:$CIRCLE_SHA1 .

test:
  post:
    - echo
      
deployment:
  master:
    branch: master
    commands:
      - ./push_container.sh
